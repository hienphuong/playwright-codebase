## 1. Đặt tên file   
  → LoginPage.ts, không phải login-page.ts hay loginpage.ts
## 2. Đặt tên class  
  → LoginPage, không phải loginPage hay login_page
## 3. Đặt tên locator  
→ usernameInput, không phải username_input hay Username
## 4. Đặt tên hàm     
 → login(), clickLoginButton(), getErrorMessage()
## 5. Đặt tên test    
 → "should login successfully with valid credentials"
## 6. Dùng async/await 
→ Dùng async/await luôn luôn, không dùng .then()
## 7. Không hardcode  
 → username, password phải lấy từ env
class LoginPage {
  
  // 1. LOCATORS — định nghĩa các element trên trang
  usernameInput
  passwordInput
  loginButton

  // 2. CONSTRUCTOR — khởi tạo page
  constructor(page)

  // 3. ACTIONS — các hành động có thể làm trên trang
  login(username, password)
  getErrorMessage()
}